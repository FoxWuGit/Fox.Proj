@model Fox.Model.ViewModel.Error.ErrorVM
@{
    ViewBag.Title = "錯誤頁";
}

<h2>錯誤頁面</h2>

<div class="form-group" style="margin-top: 10px">
    <label for="studentName">錯誤代碼：</label>
    <p>@Html.Raw(Model.DisplayCode)</p>
    
</div>
<div class="form-group">
    <label for="studentId">內容:</label>
    <p>@Html.Raw(Model.Message)</p>
</div>

<a href="@Url.Action("Index","Student")" class="btn btn-link">回首頁</a>
